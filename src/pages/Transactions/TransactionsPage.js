import React from 'react';
import RoutesBuilder from '../../routes/RoutesBuilder';
import routes from './routes';

const TransactionsPage = () => <RoutesBuilder routes={routes} />;

export default TransactionsPage;

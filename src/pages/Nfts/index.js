import React from 'react';
import RoutesBuilder from '../../routes/RoutesBuilder';
import routes from './routes';

const NtfsSection = () => <RoutesBuilder routes={routes} />;

export default NtfsSection;
